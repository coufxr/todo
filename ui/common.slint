export struct TodoItem  {
    title: string,
    checked: bool,
}

export enum Actions {
    Rename,
    Clone,
    Delete,
    Move
}

export struct OptionItem {
    title: string,
    action: Actions,
}
